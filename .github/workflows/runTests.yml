name: CI

on: 
  push:
    branches:
      - main

jobs:
  build:

    runs-on: macOS-10.15
    
    steps:
    - uses: actions/checkout@master
    - name: Start xcodebuild test
      run: xcrun instruments devices
